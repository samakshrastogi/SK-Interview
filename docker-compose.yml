version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: sk_careerhub_mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - careerhub_network
    restart: unless-stopped

  redis:
    image: redis:7.0-alpine
    container_name: sk_careerhub_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - careerhub_network
    restart: unless-stopped

volumes:
  mongo_data:
  redis_data:

networks:
  careerhub_network:
    driver: bridge
